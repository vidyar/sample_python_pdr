language: python

python:
  - 3.4

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=quay.io/shippablequay/hitchcock2_sample_python .

 

  ci:
    - ps -eax
    - ls -al
    - sudo apt-get update

  post_ci:
    - docker build -t=quay.io/shippablequay/hitchcock2_sample_python .
    - docker push quay.io/shippablequay/hitchcock2_sample_python
    - docker build --rm -t=quay.io/shippablequay/sample_python_pdr:test .
    - docker build --rm -t=quay.io/shippablequay/sample_python_pdr:qa .
    - docker push quay.io/shippablequay/sample_python_pdr:test
    - docker push quay.io/shippablequay/sample_python_pdr:qa


integrations:
  hub:
    - integrationName: ship-pdr
      type: private docker registry
      
